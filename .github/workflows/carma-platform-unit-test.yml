name: CARMA Platform ROS2 Script Python Unit Test

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [develop, master, arc-54-control-analysis-scripts-run-all-crosstrack] #TODO

jobs:
  test_and_sonar:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov numpy matplotlib pyYAML scipy
        # Add the ROS2 GPG key
        sudo curl -sSL https://raw.githubusercontent.com/ros/rosdistro/master/ros.key -o /usr/share/keyrings/ros-archive-keyring.gpg

        # Add the repository to sources list
        echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/ros-archive-keyring.gpg] http://packages.ros.org/ros2/ubuntu $(. /etc/os-release && echo $UBUNTU_CODENAME) main" | sudo tee /etc/apt/sources.list.d/ros2.list > /dev/null
 
        sudo apt-get update
        sudo apt-get install -y ros-humble-rosbag2* ros-humble-rclpy*
        export PYTHONPATH=$PYTHONPATH:/opt/ros/humble/local/lib/python3.10/dist-packages
        echo "$(pwd)/src/carma-platform/"
        export PYTHONPATH=$PYTHONPATH:"$(pwd)/src/carma-platform/"

    - name: Run tests
      run: |
        source /opt/ros/humble/setup.bash
        python -m pytest src/carma-platform/test --cov=. --cov-report=xml:./coverage.xml --junitxml=./test-results.xml --cov-context=test
        ls -la
        
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        args: >
          -Dsonar.organization=usdot-fhwa-stol
          -Dsonar.projectKey=usdot-fhwa-stol_carma-analytics-fotda
          -Dsonar.sources=src/carma-platform
          -Dsonar.tests=src/carma-platform/test
          -Dsonar.exclusions=src/carma-platform/test/**
          -Dsonar.python.coverage.reportPaths=./coverage.xml
          -Dsonar.python.xunit.reportPath=./test-results.xml
          -Dsonar.python.version=3.10
          -Dsonar.verbose=true

    - name: Debug
      run: |
        ls -la src/carma-platform
        cat /home/runner/work/carma-analytics-fotda/carma-analytics-fotda/test-results.xml
        cat /home/runner/work/carma-analytics-fotda/carma-analytics-fotda/coverage.xml

    - name: Check Quality Gate
      uses: sonarsource/sonarqube-quality-gate-action@master
      timeout-minutes: 5
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}